# Какие HTML атрибуты можно использовать для улучшения доступности?

Существует множество HTML атрибутов, которые помогают улучшить доступность веб-сайтов. Они делятся на несколько категорий: стандартные HTML атрибуты, ARIA атрибуты и специальные атрибуты для форм и медиа.

## Стандартные HTML атрибуты

### 1. `alt` (для изображений)

```html
<!-- ✅ Хорошо - информативный alt -->
<img src="chart.png" alt="График продаж за 2023 год показывает рост на 25%">

<!-- ✅ Декоративное изображение -->
<img src="decoration.png" alt="">

<!-- ❌ Плохо - отсутствует alt -->
<img src="logo.png">
```

### 2. `lang` (язык контента)

```html
<!-- ✅ Указание языка страницы -->
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Мой сайт</title>
</head>
<body>
  <p>Привет, мир!</p>
  
  <!-- Изменение языка для части контента -->
  <p>Hello, <span lang="en">world</span>!</p>
</body>
</html>
```

### 3. `title` (всплывающая подсказка)

```html
<!-- ✅ Дополнительная информация -->
<a href="/page" title="Подробная информация о продукте">
  Продукт
</a>

<abbr title="Всемирная паутина">WWW</abbr>
```

### 4. `tabindex` (порядок навигации)

```html
<!-- ✅ Управление порядком фокуса -->
<button tabindex="0">Обычная кнопка</button>
<button tabindex="1">Первая в порядке</button>
<button tabindex="2">Вторая в порядке</button>

<!-- ✅ Удалить из навигации -->
<div tabindex="-1">Не доступно с клавиатуры</div>
```

### 5. `id` и `for` (связь элементов)

```html
<!-- ✅ Связь label и input -->
<label for="email">Email</label>
<input type="email" id="email" name="email">

<!-- ✅ Связь описания и элемента -->
<input 
  type="text" 
  id="username"
  aria-describedby="username-help"
>
<span id="username-help">
  Имя пользователя должно содержать минимум 3 символа
</span>
```

## ARIA атрибуты

### 1. `aria-label` (метка элемента)

```html
<!-- ✅ Кнопка без видимого текста -->
<button aria-label="Закрыть модальное окно">
  ×
</button>

<!-- ✅ Иконка с описанием -->
<span aria-label="Рейтинг: 5 из 5 звезд">
  <span aria-hidden="true">⭐⭐⭐⭐⭐</span>
</span>
```

### 2. `aria-labelledby` (ссылка на метку)

```html
<!-- ✅ Связь с заголовком -->
<div role="dialog" aria-labelledby="modal-title">
  <h2 id="modal-title">Подтверждение</h2>
  <p>Вы уверены?</p>
</div>
```

### 3. `aria-describedby` (дополнительное описание)

```html
<!-- ✅ Описание для input -->
<label for="password">Пароль</label>
<input 
  type="password" 
  id="password"
  aria-describedby="password-help"
>
<span id="password-help">
  Пароль должен содержать минимум 8 символов
</span>
```

### 4. `aria-required` (обязательное поле)

```html
<!-- ✅ Обязательное поле -->
<label for="email">Email</label>
<input 
  type="email" 
  id="email"
  required
  aria-required="true"
>
```

### 5. `aria-invalid` (состояние валидности)

```html
<!-- ✅ Поле с ошибкой -->
<input 
  type="email" 
  id="email"
  aria-invalid="true"
  aria-describedby="email-error"
>
<span id="email-error" role="alert">
  Пожалуйста, введите корректный email
</span>
```

### 6. `aria-live` (живые области)

```html
<!-- ✅ Динамически обновляемый контент -->
<div role="status" aria-live="polite" aria-atomic="true">
  Форма успешно отправлена
</div>

<div role="alert" aria-live="assertive">
  Ошибка: неверный email
</div>
```

### 7. `aria-hidden` (скрытие от скринридеров)

```html
<!-- ✅ Декоративные элементы -->
<div class="decoration" aria-hidden="true">
  <span>✨</span>
</div>

<!-- ✅ Иконки в кнопках -->
<button aria-label="Удалить">
  <span class="icon" aria-hidden="true">🗑️</span>
</button>
```

### 8. `role` (роль элемента)

```html
<!-- ✅ Кастомные виджеты -->
<div role="button" tabindex="0" onclick="submit()">
  Отправить
</div>

<div role="dialog" aria-modal="true">
  <h2>Диалог</h2>
</div>
```

## Атрибуты для форм

### 1. `autocomplete` (автозаполнение)

```html
<!-- ✅ Автозаполнение для форм -->
<input 
  type="text" 
  name="name"
  autocomplete="name"
>

<input 
  type="email" 
  name="email"
  autocomplete="email"
>

<input 
  type="tel" 
  name="phone"
  autocomplete="tel"
>
```

### 2. `required` (обязательное поле)

```html
<!-- ✅ Обязательное поле -->
<label for="email">Email <span aria-label="обязательное поле">*</span></label>
<input 
  type="email" 
  id="email"
  name="email"
  required
  aria-required="true"
>
```

### 3. `pattern` (валидация)

```html
<!-- ✅ Паттерн для валидации -->
<input 
  type="text"
  pattern="[A-Za-z]{3,}"
  title="Минимум 3 латинские буквы"
  aria-describedby="pattern-help"
>
<span id="pattern-help">
  Введите минимум 3 латинские буквы
</span>
```

## Атрибуты для медиа

### 1. `controls` (элементы управления)

```html
<!-- ✅ Видео с элементами управления -->
<video controls>
  <source src="video.mp4" type="video/mp4">
</video>
```

### 2. `preload` (предзагрузка)

```html
<!-- ✅ Предзагрузка метаданных -->
<video preload="metadata">
  <source src="video.mp4" type="video/mp4">
</video>
```

## Семантические атрибуты

### 1. `scope` (для таблиц)

```html
<!-- ✅ Область действия заголовка -->
<table>
  <thead>
    <tr>
      <th scope="col">Название</th>
      <th scope="col">Цена</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">Продукт 1</th>
      <td>1000 ₽</td>
    </tr>
  </tbody>
</table>
```

### 2. `headers` (для таблиц)

```html
<!-- ✅ Связь ячеек с заголовками -->
<table>
  <thead>
    <tr>
      <th id="name">Название</th>
      <th id="price">Цена</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td headers="name">Продукт 1</td>
      <td headers="price">1000 ₽</td>
    </tr>
  </tbody>
</table>
```

## Практические примеры

### Пример 1: Доступная форма

```html
<form>
  <fieldset>
    <legend>Контактная информация</legend>
    
    <label for="name">
      Имя 
      <span aria-label="обязательное поле">*</span>
    </label>
    <input 
      type="text" 
      id="name"
      name="name"
      required
      aria-required="true"
      aria-describedby="name-help"
      autocomplete="name"
    >
    <span id="name-help" class="help-text">
      Введите ваше полное имя
    </span>
    
    <label for="email">
      Email 
      <span aria-label="обязательное поле">*</span>
    </label>
    <input 
      type="email" 
      id="email"
      name="email"
      required
      aria-required="true"
      aria-invalid="false"
      aria-describedby="email-error email-help"
      autocomplete="email"
    >
    <span id="email-help" class="help-text">
      Мы не будем делиться вашим email
    </span>
    <span id="email-error" role="alert" aria-live="polite"></span>
  </fieldset>
  
  <button type="submit">Отправить</button>
</form>
```

### Пример 2: Доступная навигация

```html
<nav aria-label="Основная навигация">
  <ul>
    <li>
      <a href="/" aria-current="page">Главная</a>
    </li>
    <li>
      <a href="/products">Продукты</a>
    </li>
    <li>
      <a href="/about">О нас</a>
    </li>
  </ul>
</nav>
```

### Пример 3: Доступное модальное окно

```html
<div 
  role="dialog"
  aria-labelledby="modal-title"
  aria-describedby="modal-description"
  aria-modal="true"
>
  <h2 id="modal-title">Подтверждение</h2>
  <p id="modal-description">
    Вы уверены, что хотите удалить этот элемент?
  </p>
  <button aria-label="Подтвердить удаление">Да</button>
  <button aria-label="Отменить удаление">Нет</button>
</div>
```

## Лучшие практики

### ✅ Делайте:

1. **Используйте семантические атрибуты** — `alt`, `lang`, `title`
2. **Добавляйте ARIA атрибуты** — когда стандартного HTML недостаточно
3. **Связывайте элементы** — используйте `id`, `for`, `aria-labelledby`, `aria-describedby`
4. **Указывайте состояния** — `aria-invalid`, `aria-required`, `aria-current`

### ❌ Не делайте:

1. **Не дублируйте информацию** — не используйте `aria-label` и видимый текст для одного и того же
2. **Не используйте избыточные атрибуты** — не добавляйте `role="button"` к элементу `<button>`
3. **Не скрывайте важную информацию** — не используйте `aria-hidden="true"` на важных элементах

## Заключение

HTML атрибуты для доступности можно разделить на категории:

- **Стандартные HTML** — `alt`, `lang`, `title`, `tabindex`
- **ARIA атрибуты** — `aria-label`, `aria-describedby`, `aria-required`, `role`
- **Атрибуты форм** — `autocomplete`, `required`, `pattern`
- **Атрибуты медиа** — `controls`, `preload`

**Помните:** используйте атрибуты осознанно и тестируйте с помощью скринридеров. Правильное использование атрибутов значительно улучшает доступность веб-сайтов для всех пользователей.


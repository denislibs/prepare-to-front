# Разница между role и aria-label в контексте accessibility?

`role` и `aria-label` — это два разных ARIA атрибута, которые служат разным целям в контексте доступности. Понимание разницы между ними важно для правильного использования ARIA.

## role

`role` — это атрибут, который определяет **тип элемента** или **его функциональную роль** для вспомогательных технологий, таких как скринридеры.

### Назначение

- Определяет, **что это за элемент** (кнопка, диалог, меню и т.д.)
- Указывает **поведение элемента** для скринридеров
- Заменяет или дополняет семантику HTML элемента

### Примеры

```html
<!-- ✅ Определение роли кнопки -->
<div role="button" tabindex="0" onclick="submit()">
  Отправить
</div>
<!-- Скринридер прочитает: "Отправить, кнопка" -->

<!-- ✅ Определение роли диалога -->
<div role="dialog" aria-labelledby="modal-title">
  <h2 id="modal-title">Подтверждение</h2>
</div>
<!-- Скринридер прочитает: "Подтверждение, диалоговое окно" -->

<!-- ✅ Определение роли меню -->
<div role="menu" aria-label="Действия">
  <button role="menuitem">Редактировать</button>
  <button role="menuitem">Удалить</button>
</div>
<!-- Скринридер прочитает: "Действия, меню" -->
```

## aria-label

`aria-label` — это атрибут, который предоставляет **доступное имя** (accessible name) для элемента, когда видимый текст недоступен или недостаточен.

### Назначение

- Предоставляет **текстовую метку** для элемента
- Используется, когда элемент не имеет видимого текста
- Предоставляет **более описательное имя** для скринридеров

### Примеры

```html
<!-- ✅ Кнопка без видимого текста -->
<button aria-label="Закрыть модальное окно">
  ×
</button>
<!-- Скринридер прочитает: "Закрыть модальное окно, кнопка" -->

<!-- ✅ Иконка с описанием -->
<span aria-label="Рейтинг: 5 из 5 звезд">
  <span aria-hidden="true">⭐⭐⭐⭐⭐</span>
</span>
<!-- Скринридер прочитает: "Рейтинг: 5 из 5 звезд" -->

<!-- ✅ Ссылка с описанием -->
<a href="/cart" aria-label="Перейти в корзину (3 товара)">
  <span aria-hidden="true">🛒</span>
</a>
<!-- Скринридер прочитает: "Перейти в корзину (3 товара), ссылка" -->
```

## Ключевые различия

### 1. Назначение

**role:**
- Определяет **тип элемента** (что это?)
- Указывает **поведение** для скринридеров

**aria-label:**
- Предоставляет **имя элемента** (как называется?)
- Описывает **назначение** элемента

### 2. Что они изменяют

**role:**
- Изменяет **семантику** элемента
- Указывает **роль** для скринридеров

**aria-label:**
- Изменяет **доступное имя** элемента
- Заменяет или дополняет видимый текст

### 3. Когда используются

**role:**
- Когда элемент используется не по назначению
- Когда семантический HTML недостаточен
- Для кастомных виджетов

**aria-label:**
- Когда элемент не имеет видимого текста
- Когда видимый текст недостаточен
- Для улучшения описания

## Практические примеры

### Пример 1: Комбинация role и aria-label

```html
<!-- ✅ Кастомная кнопка -->
<div 
  role="button"
  aria-label="Добавить товар в корзину"
  tabindex="0"
  onclick="addToCart()"
>
  <span class="icon" aria-hidden="true">🛒</span>
</div>
<!-- Скринридер: "Добавить товар в корзину, кнопка" -->
```

### Пример 2: Разница в использовании

```html
<!-- ✅ role определяет тип -->
<div role="button" onclick="submit()">
  Отправить
</div>
<!-- Скринридер: "Отправить, кнопка" -->

<!-- ✅ aria-label предоставляет имя -->
<button aria-label="Закрыть форму">
  ×
</button>
<!-- Скринридер: "Закрыть форму, кнопка" -->

<!-- ✅ Комбинация обоих -->
<div 
  role="button"
  aria-label="Сохранить изменения"
  onclick="save()"
>
  <span aria-hidden="true">💾</span>
</div>
<!-- Скринридер: "Сохранить изменения, кнопка" -->
```

### Пример 3: Когда использовать каждый

```html
<!-- ✅ role - когда элемент не семантический -->
<div role="button" tabindex="0" onclick="click()">
  Нажми меня
</div>

<!-- ✅ aria-label - когда нет видимого текста -->
<button aria-label="Удалить элемент">
  <span aria-hidden="true">🗑️</span>
</button>

<!-- ✅ Оба вместе - кастомный виджет -->
<div 
  role="checkbox"
  aria-label="Согласен с условиями"
  aria-checked="false"
  tabindex="0"
  onclick="toggle()"
>
  <span aria-hidden="true">☐</span>
</div>
```

## Взаимодействие

### role + aria-label

```html
<!-- ✅ Часто используются вместе -->
<div 
  role="button"
  aria-label="Открыть меню настроек"
  tabindex="0"
  onclick="openSettings()"
>
  <span class="icon" aria-hidden="true">⚙️</span>
</div>
<!-- role: определяет тип (кнопка) -->
<!-- aria-label: предоставляет имя (Открыть меню настроек) -->
```

### Приоритет доступного имени

Когда скринридер определяет имя элемента, он использует следующий порядок:

1. `aria-label` (если указан)
2. `aria-labelledby` (если указан)
3. Видимый текст элемента
4. `alt` текст (для изображений)
5. `title` атрибут
6. `placeholder` (для некоторых элементов)

```html
<!-- ✅ aria-label имеет приоритет -->
<button aria-label="Закрыть">
  Отправить
</button>
<!-- Скринридер прочитает: "Закрыть" (не "Отправить") -->
```

## Лучшие практики

### ✅ Правильное использование role

```html
<!-- ✅ Когда элемент не семантический -->
<div role="button" tabindex="0" onclick="submit()">
  Отправить
</div>

<!-- ✅ Для кастомных виджетов -->
<div role="dialog" aria-modal="true">
  <h2>Диалог</h2>
</div>
```

### ✅ Правильное использование aria-label

```html
<!-- ✅ Когда нет видимого текста -->
<button aria-label="Закрыть">
  ×
</button>

<!-- ✅ Когда видимый текст недостаточен -->
<a href="/cart" aria-label="Корзина (3 товара)">
  🛒
</a>
```

### ❌ Неправильное использование

```html
<!-- ❌ Избыточное использование role -->
<button role="button" onclick="submit()">
  Отправить
</button>
<!-- role="button" избыточен для <button> -->

<!-- ❌ aria-label вместо видимого текста -->
<button aria-label="Отправить форму">
  <!-- Пусто -->
</button>
<!-- Лучше использовать видимый текст -->
```

## Заключение

Разница между `role` и `aria-label`:

- **role** — определяет **тип элемента** (что это?)
- **aria-label** — предоставляет **имя элемента** (как называется?)
- **role** — изменяет семантику
- **aria-label** — изменяет доступное имя
- **Часто используются вместе** — для кастомных виджетов

**Помните:**
- Используйте `role` для определения типа элемента
- Используйте `aria-label` для предоставления имени
- Всегда предпочитайте семантический HTML
- Тестируйте со скринридерами

Правильное использование обоих атрибутов помогает создать доступные и понятные интерфейсы для всех пользователей.


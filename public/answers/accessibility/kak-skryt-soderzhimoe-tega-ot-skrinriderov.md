# Как скрыть содержимое тэга от скринридеров?

Иногда необходимо скрыть визуальный контент от скринридеров, например, декоративные элементы или дублирующую информацию. Существует несколько способов сделать это правильно.

## Методы скрытия контента от скринридеров

### 1. Атрибут `aria-hidden="true"`

Самый простой и рекомендуемый способ скрыть элемент от скринридеров.

```html
<!-- ✅ Декоративные элементы -->
<div class="decoration" aria-hidden="true">
  <span>✨</span>
  <span>🎉</span>
</div>

<!-- ✅ Иконки в кнопках (когда есть текстовый label) -->
<button aria-label="Удалить элемент">
  <span class="icon" aria-hidden="true">🗑️</span>
  Удалить
</button>

<!-- ✅ Дублирующий визуальный контент -->
<div class="breadcrumb" aria-hidden="true">
  <span>Главная > Продукты > Категория</span>
</div>
<nav aria-label="Хлебные крошки">
  <ol>
    <li><a href="/">Главная</a></li>
    <li><a href="/products">Продукты</a></li>
    <li>Категория</li>
  </ol>
</nav>
```

**Важно:** `aria-hidden="true"` скрывает элемент и все его дочерние элементы от скринридеров.

### 2. Пустой атрибут `alt=""` для изображений

Для декоративных изображений используйте пустой `alt`.

```html
<!-- ✅ Декоративное изображение -->
<img src="decoration.png" alt="">

<!-- ✅ Или с role="presentation" -->
<img src="decoration.png" alt="" role="presentation">

<!-- ❌ Плохо - скринридер прочитает "изображение" -->
<img src="decoration.png">
```

### 3. CSS класс `.sr-only` (Screen Reader Only)

Скрыть элемент визуально, но оставить доступным для скринридеров (обратная задача).

```css
/* Скрыть визуально, но оставить для скринридеров */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```

```html
<!-- ✅ Скрыть визуально, но доступно для скринридеров -->
<button>
  <span class="icon">🔍</span>
  <span class="sr-only">Поиск</span>
</button>

<!-- ✅ Дополнительный контекст для скринридеров -->
<form>
  <label for="search">Поиск</label>
  <input type="search" id="search" aria-describedby="search-help">
  <span id="search-help" class="sr-only">
    Введите ключевое слово для поиска по сайту
  </span>
</form>
```

### 4. CSS `display: none` и `visibility: hidden`

Эти методы скрывают элементы и от скринридеров, и визуально.

```css
/* Скрыть полностью (и визуально, и от скринридеров) */
.hidden {
  display: none;
}

.visually-hidden {
  visibility: hidden;
}
```

```html
<!-- ✅ Скрыть элемент полностью -->
<div class="hidden" aria-hidden="true">
  Временный контент
</div>
```

**Важно:** Используйте эти методы только когда нужно скрыть элемент полностью.

## Практические примеры

### Пример 1: Декоративные иконки

```html
<!-- ✅ Хорошо - иконка скрыта, но кнопка доступна -->
<button aria-label="Закрыть модальное окно">
  <span class="icon" aria-hidden="true">×</span>
</button>

<!-- ✅ Альтернатива - текст + иконка -->
<button>
  <span class="icon" aria-hidden="true">×</span>
  <span class="sr-only">Закрыть</span>
</button>
```

### Пример 2: Дублирующий контент

```html
<!-- ✅ Визуальный индикатор скрыт от скринридера -->
<div class="status-indicator" aria-hidden="true">
  <span class="dot"></span>
</div>

<!-- ✅ Текстовая информация для скринридера -->
<span class="sr-only">Статус: Активен</span>
```

### Пример 3: Визуальные разделители

```html
<!-- ✅ Декоративный разделитель -->
<hr aria-hidden="true" class="decorative-divider">

<!-- ✅ Или с CSS -->
<div class="divider" aria-hidden="true"></div>
```

### Пример 4: Сложные компоненты

```html
<!-- ✅ Карточка продукта -->
<article>
  <div class="product-image" aria-hidden="true">
    <img src="product.jpg" alt="">
  </div>
  
  <div class="product-info">
    <h2>Название продукта</h2>
    <p>Описание продукта</p>
    
    <!-- Декоративный рейтинг скрыт -->
    <div class="stars" aria-hidden="true">
      <span>⭐⭐⭐⭐⭐</span>
    </div>
    
    <!-- Текстовый рейтинг для скринридера -->
    <span class="sr-only">Рейтинг: 5 из 5 звезд</span>
  </div>
</article>
```

## Когда НЕ нужно скрывать

### ❌ Не скрывайте важную информацию

```html
<!-- ❌ Плохо - скрыта важная информация -->
<div aria-hidden="true">
  <p>Важное предупреждение!</p>
</div>

<!-- ✅ Хорошо - информация доступна -->
<div role="alert">
  <p>Важное предупреждение!</p>
</div>
```

### ❌ Не скрывайте интерактивные элементы

```html
<!-- ❌ Плохо - кнопка скрыта -->
<button aria-hidden="true" onclick="submit()">
  Отправить
</button>

<!-- ✅ Хорошо - кнопка доступна -->
<button onclick="submit()">
  Отправить
</button>
```

### ❌ Не скрывайте элементы формы

```html
<!-- ❌ Плохо - поле скрыто -->
<input type="hidden" name="token" value="123">
<div aria-hidden="true">
  <label for="email">Email</label>
  <input type="email" id="email">
</div>

<!-- ✅ Хорошо - поле доступно -->
<label for="email">Email</label>
<input type="email" id="email" name="email">
```

## Динамическое управление `aria-hidden`

### JavaScript примеры

```javascript
// Скрыть элемент от скринридеров
function hideFromScreenReader(element) {
  element.setAttribute('aria-hidden', 'true');
}

// Показать элемент скринридерам
function showToScreenReader(element) {
  element.removeAttribute('aria-hidden');
}

// Пример использования
const modal = document.getElementById('modal');
const overlay = document.getElementById('overlay');

// При открытии модального окна
function openModal() {
  modal.setAttribute('aria-hidden', 'false');
  modal.setAttribute('aria-modal', 'true');
  overlay.setAttribute('aria-hidden', 'true');
  
  // Скрыть основной контент от скринридеров
  const mainContent = document.querySelector('main');
  mainContent.setAttribute('aria-hidden', 'true');
}

// При закрытии модального окна
function closeModal() {
  modal.setAttribute('aria-hidden', 'true');
  overlay.setAttribute('aria-hidden', 'true');
  
  // Показать основной контент скринридерам
  const mainContent = document.querySelector('main');
  mainContent.removeAttribute('aria-hidden');
}
```

## Проверка скрытия

### Тестирование со скринридером

1. **Установите NVDA или VoiceOver**
2. **Навигация по странице** — скрытые элементы не должны озвучиваться
3. **Проверка структуры** — используйте команды для просмотра структуры страницы

### Инструменты

- **WAVE** — показывает элементы с `aria-hidden="true"`
- **axe DevTools** — предупреждает о неправильном использовании `aria-hidden`
- **Lighthouse** — проверяет доступность

## Лучшие практики

### ✅ Делайте:

1. Используйте `aria-hidden="true"` для декоративных элементов
2. Используйте пустой `alt=""` для декоративных изображений
3. Скрывайте дублирующий визуальный контент
4. Управляйте `aria-hidden` динамически для модальных окон

### ❌ Не делайте:

1. Не скрывайте важную информацию
2. Не скрывайте интерактивные элементы
3. Не скрывайте элементы формы (кроме скрытых полей)
4. Не используйте `aria-hidden` на родительском элементе, если дочерние элементы должны быть доступны

## Заключение

Правильное скрытие контента от скринридеров улучшает пользовательский опыт для людей, использующих вспомогательные технологии:

- **`aria-hidden="true"`** — основной метод для скрытия элементов
- **Пустой `alt=""`** — для декоративных изображений
- **`.sr-only`** — для скрытия визуально, но доступности для скринридеров
- **Динамическое управление** — для модальных окон и динамического контента

Помните: скрывайте только декоративный или дублирующий контент, никогда не скрывайте важную информацию или интерактивные элементы.

